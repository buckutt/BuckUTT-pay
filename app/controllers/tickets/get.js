// Pay - /app/controllers/tickets/index.js

// Tickets getter

'use strict';

module.exports = function (db) {
    return function (req, res) {
        res.json({
            'foo': 'bar'
        });
    };
};
